// SPDX-License-Identifier: EUPL-1.2
// Affine Mode: Implicit Drop Example
//
// This demonstrates how affine mode allows values to be dropped
// implicitly without explicit consumption.
//
// Try switching to LINEAR mode - this will produce a type error!

fn main() -> i32 {
    // Create a value
    let x = 42;

    // In AFFINE mode:
    //   ✅ x can be implicitly dropped
    //   ✅ No error if x is not used

    // In LINEAR mode:
    //   ❌ Type error: x not consumed
    //   💡 Fix: add `drop(x)` before end of scope

    // Return without using x
    0
}
